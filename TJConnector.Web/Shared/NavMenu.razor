<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">TJConnector</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="batches">
                <span class="bi bi-folder" style="font-size: 14px;" aria-hidden="true"></span> Order Batches
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="emission">
                <span class="bi bi-folder" style="font-size: 14px;" aria-hidden="true"></span> External Orders
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="containers">
                <span class="bi bi-folder" style="font-size: 14px;" aria-hidden="true"></span> Aggregations
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="products">
                <span class="bi bi-folder" style="font-size: 14px;" aria-hidden="true"></span> Product
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="ContainerInfo">
                <span class="bi bi-folder" style="font-size: 14px;" aria-hidden="true"></span> Container Info
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
