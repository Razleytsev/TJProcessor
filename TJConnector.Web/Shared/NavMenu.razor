@* Sidebar adopted from the Changer reference: single dark surface,
   rounded nav items, accent left-bar on the active route, version tag
   at the foot. Test-Run link is admin-only (same pattern as Changer's
   Settings link). *@

<div class="sidebar">
    <a href="/" class="brand" aria-label="Abdulla — go to home">Abdulla</a>

    <nav class="nav-scrollable">
        <NavLink class="nav-link" href="batches">
            Order Batches
        </NavLink>
        <NavLink class="nav-link" href="containers">
            Submission Requests
        </NavLink>
        <NavLink class="nav-link" href="products">
            Products
        </NavLink>
        <AuthorizeView Roles="Admin">
            <NavLink class="nav-link" href="test-run">
                Test Run
            </NavLink>
        </AuthorizeView>
    </nav>

    <div class="nav-footer">
        <span class="version-tag">v1.0</span>
    </div>
</div>

@code {
    private bool collapseNavMenu = true;
    private void ToggleNavMenu() => collapseNavMenu = !collapseNavMenu;
}
