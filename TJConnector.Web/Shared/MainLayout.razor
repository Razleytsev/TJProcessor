@inherits LayoutComponentBase

<HeadContent>
    <meta name="application-name" content="Abdulla" />
</HeadContent>
<PageTitle>Abdulla</PageTitle>

<div class="page">
    <NavMenu />
    <main class="main-content">
        @* Top-bar removed: the deployment-contour tag moved into the fixed
           sidebar foot (NavMenu), on its own row ABOVE the version + user. This
           reclaims the full content height and removes the only top chrome. *@
        <div class="content-area">
            @Body
        </div>
    </main>
</div>
